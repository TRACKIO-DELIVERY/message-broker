name: CI Message Broker

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop
  workflow_dispatch:

jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      
      - name: Install Docker Compose
        run: |
          sudo curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          sudo chmod +x /usr/local/bin/docker-compose
          docker-compose --version
      
      - name: Create Docker Network
        run: docker network create --driver bridge rabbitmq_net || true
      
      - name: Test RabbitMQ connection
        run: |
          docker-compose -f docker-compose.yml up -d
          sleep 15
          docker-compose -f docker-compose.yml exec rabbitmq rabbitmq-diagnostics ping
  
  sync_notify_trackio:
    if : github.event_name == 'push' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    needs: ci
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Sync and notify Track.io
        env:
          PARENT_REPO: TRACKIO-DELIVERY/trackio
        run: |
          curl -L \
          -X POST \
          -H "Accept: application/vnd.github+json" \
          -H "Authorization: Bearer ${{ secrets.GH_TOKEN }}" \
          -H "X-GitHub-Api-Version: 2022-11-28" \
          https://api.github.com/repos/${{ env.PARENT_REPO }}/dispatches \
          -d '{"event_type":"message-broker-updated","client_payload":\
          {"ref":"'"${{ github.ref }}"'","sha":"'"${{ github.sha }}"'",\
          "repo":"'"${{ github.repository }}"'}'